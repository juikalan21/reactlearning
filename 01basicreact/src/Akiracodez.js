function Two(){
    return(
        <h2>coding in react</h2>
    )
}

export default Two

//use vite or react - always start the function name with an uppercase
// keep other js/jsx files also in capital
// files in vite must always be jsx
